#include "main.h"

/**
* print_numbers - prints numbers from 0 to 9
* Return:0
*/

	void print_most_numbers(void)
	{
	char x = 0;

	while (x <= 9) && (x != 2) && (x !=4)
	{
		_putchar(x + '0');
		x++;

	}
		_putchar('\n');

}
